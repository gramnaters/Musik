# Tidal-style PlayerBar

Drop-in React component. Requirements:
- React 18+
- TailwindCSS
- lucide-react

## Usage
```tsx
import PlayerBar from "./PlayerBar";

<PlayerBar />
```
Renders as a fixed, glassmorphic pill at the bottom of the viewport.
